const names = ['Linus', 'Ada', 'Bill']

for (let i = 0; i < names.length; i++) {
    //console.log(names[i])
    const HTMLTemplate = `<p>${names[i]}</p>`
    console.log(HTMLTemplate)
}

const nomes = ['Jobs', 'Hopper', 'Turing']
let i = 0

while(i<nomes.length) {
    console.log(nomes[i])
    i++
}

